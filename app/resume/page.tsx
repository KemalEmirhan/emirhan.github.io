import React from 'react';
import Card from '@/components/Card';

const Resume = () => {
  return (
    <Card>
      <p className='text-center'>Resume will be here soon(no worries)...</p>
    </Card>
  );
};

export default Resume;
